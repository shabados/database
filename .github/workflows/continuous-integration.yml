name: Continuous Integration

on:
  push:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      - run: npm run lint

  validate-collections:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      - run: npm run collections:validate

  build-database:
    runs-on: ubuntu-latest
    needs:
      - lint
      - validate-collections
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      - run: npm run build-database
      - uses: actions/upload-artifact@v4
        with:
          name: database.sqlite
          path: dist/database.sqlite

  analyze-database:
    runs-on: ubuntu-latest
    needs:
      - build-database
    steps:
      - run: brew install sqlite-analyzer
      - uses: actions/download-artifact@v4
        with:
          name: database.sqlite
          path: dist/database.sqlite
      - run: sqlite-analyzer dist/database.sqlite
